# PostgreSQL connection (SQLAlchemy async format — used by FastAPI and Alembic)
DATABASE_URL=postgresql+asyncpg://domq:domq_dev@localhost:5432/domq

# asyncpg native format (no +asyncpg prefix — used by DatabaseManager directly)
DATABASE_DSN=postgresql://domq:domq_dev@localhost:5432/domq
