services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: domq
      POSTGRES_PASSWORD: domq_dev
      POSTGRES_DB: domq
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
